# 移动端交互重构 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `<640px` 视口下为 TaoStudio Image Lab 提供"底部 Tab + 全屏内容 + 抽屉式创作"的移动端独立 IA，桌面端（≥640px）渲染路径零视觉/交互改动。

**Architecture:** 响应式双前缀策略（`max-sm:` 移动 / `sm:` 桌面）。核心业务逻辑（prompt/参数/生成/上传）从 `InputBar` 等价提取为 `useImageComposer` hook，桌面与移动共用。移动端新增 4 个轻量组件 + 2 个 hook，全部用 `max-sm:` / `useIsMobile()` 隔离，桌面端组件树不动。

**Tech Stack:** React 18 + TypeScript + Vite + Tailwind CSS（默认 `sm`=640px 断点）+ Zustand（`src/store.ts`）+ vitest。

## Global Constraints

（来自 spec，每个任务隐式遵守）

- **桌面端零视觉/交互改动**：所有移动 UI 用 `max-sm:` 前缀或 `useIsMobile()` 守卫；桌面端（≥640px）不新增任何视觉或交互分支。
- **断点**：移动 = `max-width:639px`（Tailwind `max-sm:` / JS `window.innerWidth < 640`）。不新增断点。
- **UI 语言**：所有新 UI 文案用中文（spec AGENTS.md 要求）。
- **品牌**：复用 `tailwind.config.js` 已有 `studio` 色板（`peach` `#df7b57`、`marine` `#356c82`、`moss` `#6f8f72`、`canvas`/`canvas-dark`），不引入新色。
- **测试**：vitest，`describe/it/expect` 显式导入（无 globals、无 setupFiles）。用到 `window.matchMedia` 的代码必须在测试里 `vi.stubGlobal('matchMedia', ...)`。
- **hook 约定**：`src/hooks/` 现有 hook 全是 **named export + `useX` 命名 + 无 default export**。新 hook 遵守。
- **不碰**：provider/proxy/API 行为、云存储、桌面端 `InputBar` 的视觉/交互、`SettingsModal` 内容拆分（本次仅全屏 sheet 包裹）。
- **gates**：`npm test`（=`vitest run`）、`npm run lint`（=`eslint .`，零错误基线）、`npm run build`（=`tsc -b && vite build`）必须通过。

## File Structure

**新增文件：**
- `src/hooks/useIsMobile.ts` — 统一的 `useIsMobile()`，替换 `InputBar.tsx` + `HelpModal.tsx` 两处本地副本
- `src/hooks/useImageComposer.ts` — 从 `InputBar` 提取的 prompt/参数/派生/提交/上传逻辑
- `src/hooks/useMobileSheet.ts` — 抽屉上滑/下滑手势 + visualViewport 键盘适配
- `src/components/MobileShell.tsx` — 移动端外壳：`MobileTopBar` + 内容槽 + `MobileTabBar` + FAB（一个文件，三个内部子组件，因为它们强耦合且共享布局类）
- `src/components/MobileComposeSheet.tsx` — 创作抽屉（消费 `useImageComposer`）
- `src/components/MobileMoreParamsSheet.tsx` — 「+ 更多」二级 sheet（格式/透明/审核/精确尺寸）
- `src/components/__tests__/useIsMobile.test.ts` — hook 单测
- `src/components/__tests__/useImageComposer.test.ts` — hook 单测
- `src/components/__tests__/useMobileSheet.test.ts` — hook 单测

**修改文件：**
- `src/components/InputBar.tsx` — 删除本地 `useIsMobile`（改 import）；将 443-474/746-750/778-787/1011-1128/1226-1258 的逻辑替换为消费 `useImageComposer()`
- `src/components/HelpModal.tsx` — 删除本地 `useIsMobile`（改 import）
- `src/App.tsx` — 在 `<sm` 下渲染 `<MobileShell>` 替代 `<Header/> + <main> + <InputBar/>`；模态层保持不变
- `src/components/TaskGrid.tsx` — `<sm` 下单列大图卡片
- `src/components/SearchBar.tsx` — `<sm` 下顶部 sticky 折叠
- `src/components/DetailModal.tsx` — `<sm` 下全屏 sheet
- `src/components/Lightbox.tsx` — `<sm` 下全屏 + 左右滑
- `src/components/SettingsModal.tsx` — `<sm` 下全屏 sheet 包裹（内容不动）
- `src/index.css` — 新增 `.safe-area-bottom` 工具类（已确认不存在；变量 `--safe-area-bottom` 已有）

---

## Task 1: 抽取统一 `useIsMobile` hook

**目标**：消除两份逐字节相同的本地副本，为后续所有移动端组件提供唯一断点真源。纯重构、行为等价、零风险暖身。

**Files:**
- Create: `src/hooks/useIsMobile.ts`
- Create: `src/components/__tests__/useIsMobile.test.ts`
- Modify: `src/components/InputBar.tsx`（删 `:395-402` 的本地 `useIsMobile`，加 import）
- Modify: `src/components/HelpModal.tsx`（删 `:13-20` 的本地 `useIsMobile`，加 import）

**Interfaces:**
- Produces: `export function useIsMobile(): boolean` —— 返回 `window.innerWidth < 640`，监听 `resize`。

- [ ] **Step 1: 写失败测试**

`src/components/__tests__/useIsMobile.test.ts`：

```ts
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { renderHook } from '@testing-library/react'
import { useIsMobile } from '../../hooks/useIsMobile'

describe('useIsMobile', () => {
  beforeEach(() => {
    vi.unstubAllGlobals()
  })

  it('returns true when innerWidth < 640', () => {
    vi.stubGlobal('innerWidth', 390)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(true)
  })

  it('returns false when innerWidth >= 640', () => {
    vi.stubGlobal('innerWidth', 1280)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(false)
  })

  it('updates on resize', () => {
    vi.stubGlobal('innerWidth', 1280)
    const { result } = renderHook(() => useIsMobile())
    expect(result.current).toBe(false)
    vi.stubGlobal('innerWidth', 390)
    window.dispatchEvent(new Event('resize'))
    // renderHook 不会自动 re-render on 外部事件；rerun to confirm listener wired
    expect(result.current).toBe(false) // still false until re-render
  })
})
```

注意：`@testing-library/react` 需要确认是否已是依赖。

- [ ] **Step 2: 验证测试失败**

```bash
npm test -- useIsMobile
```
Expected: FAIL（`Cannot find module '../../hooks/useIsMobile'`）。若提示缺 `@testing-library/react`，先 `npm i -D @testing-library/react`。

- [ ] **Step 3: 实现 hook**

`src/hooks/useIsMobile.ts`：

```ts
import { useEffect, useState } from 'react'

const MOBILE_BREAKPOINT = 640

/**
 * 移动端断点判断。与 Tailwind `sm` (640px) 一致：
 * innerWidth < 640 视为移动端。
 */
export function useIsMobile(): boolean {
  const [isMobile, setIsMobile] = useState(
    typeof window !== 'undefined' && window.innerWidth < MOBILE_BREAKPOINT,
  )
  useEffect(() => {
    const onResize = () => setIsMobile(window.innerWidth < MOBILE_BREAKPOINT)
    window.addEventListener('resize', onResize)
    return () => window.removeEventListener('resize', onResize)
  }, [])
  return isMobile
}
```

（SSR 守卫 `typeof window !== 'undefined'` 是为了未来 SSR 安全；当前是 CSR 不影响行为。）

- [ ] **Step 4: 验证测试通过**

```bash
npm test -- useIsMobile
```
Expected: PASS（3 个 it 全过）。

- [ ] **Step 5: 替换 InputBar 本地副本**

`src/components/InputBar.tsx`：

1. 在 import 区（第 1 行的 react import 之后）加：
```ts
import { useIsMobile } from '../hooks/useIsMobile'
```
2. 删除第 395-402 行的本地 `function useIsMobile() { ... }`（整块）。

- [ ] **Step 6: 替换 HelpModal 本地副本**

`src/components/HelpModal.tsx`：

1. 在文件顶部 import 区加：
```ts
import { useIsMobile } from '../hooks/useIsMobile'
```
2. 删除第 13-20 行的本地 `function useIsMobile() { ... }`（整块）。

- [ ] **Step 7: 验证桌面端未受影响**

```bash
npm run lint
npm run build
```
Expected: lint 零错误；build 成功（tsc + vite 通过，证明类型与引用都对）。

- [ ] **Step 8: 提交**

```bash
git add src/hooks/useIsMobile.ts src/components/__tests__/useIsMobile.test.ts src/components/InputBar.tsx src/components/HelpModal.tsx
git commit -m "Extract unified useIsMobile hook

Replace two byte-identical local copies in InputBar.tsx and HelpModal.tsx
with a single src/hooks/useIsMobile.ts. Behavior-equivalent (innerWidth<640,
resize listener). Establishes the single source of truth for the mobile
breakpoint used by all subsequent mobile work."
```

---

## Task 2: 提取 `useImageComposer` hook

**目标**：把 `InputBar` 里的"prompt/参数/派生/提交/上传"逻辑等价提取为 hook，桌面 `InputBar` 与移动 `MobileComposeSheet` 共用。这是整个计划里最关键也最需要小心的一步——**行为必须等价**。

**Files:**
- Create: `src/hooks/useImageComposer.ts`
- Create: `src/components/__tests__/useImageComposer.test.ts`
- Modify: `src/components/InputBar.tsx`（用 `useImageComposer()` 替换被提取的代码段）

**Interfaces:**
- Consumes（来自 `src/store.ts`）：
  - `prompt`/`setPrompt`、`inputImages` + 5 actions（`addInputImage`/`replaceInputImage`/`removeInputImage`/`clearInputImages`/`moveInputImage`）
  - `params: TaskParams` / `setParams: (p: Partial<TaskParams>) => void`
  - `settings` / `setSettings`
  - store 外的 `submitTask()`（`src/store.ts:2387`，standalone async fn）、`submitAgentMessage`、`stopAgentResponse`、`addImageFromFile`
  - `appMode`、`activeAgentConversationId`
- Produces:
  ```ts
  export interface ImageComposer {
    // 输入
    prompt: string
    setPrompt: (p: string) => void
    inputImages: InputImage[]
    addInputImage: (...) => void   // 透传 store
    removeInputImage: (...) => void
    clearInputImages: () => void
    // 参数
    params: TaskParams
    setParams: (p: Partial<TaskParams>) => void
    // 派生（capability flags）
    displaySize: string
    outputImageLimit: number
    atImageLimit: boolean
    uploadImageTooltipText: string
    moderationDisabled: boolean
    transparentOutputAvailable: boolean
    showTransparentOutputControl: boolean
    transparentOutputEnabled: boolean
    compressionDisabled: boolean
    exactSizeDisabled: boolean
    exactSizeEnabled: boolean
    // 本地输入镜像
    outputCompressionInput: string
    setOutputCompressionInput: (v: string) => void
    nInput: string
    setNInput: (v: string) => void
    // 4K
    asset4KOriginalRatioPreset: ... // 按现状透传
    applyAsset4KOriginalRatioPreset: () => void
    applyAsset4KRatioPreset: (preset) => void
    // 提交
    submitCurrentMode: () => void
    stopActiveAgentResponse: () => void
    hasSubmitApiConfig: boolean
    activeAgentIsRunning: boolean
    // 上传
    handleFiles: (files: FileList | File[]) => Promise<void>
    // 策略展示数据（桌面端"当前生成策略"块仍需要）
    baseGenerationStrategyItems: StrategyItem[]
    generationStrategyItems: StrategyItem[]
  }
  export function useImageComposer(): ImageComposer
  ```

  > **重要**：上面的字段列表来自勘查（`InputBar.tsx` 443-474/746-750/778-787/820-860/1011-1128/1226-1258）。实现时**以 `InputBar` 现有变量名为准**逐个搬移，签名照抄。如果发现某个变量其实是纯 UI 状态（如 contentEditable 的 `cursorPos`/`menuLeft`/`atImageMenuIndex`），**不要**搬进 hook——它留在组件里。

- [ ] **Step 1: 写失败测试（行为等价性回归）**

`src/components/__tests__/useImageComposer.test.ts`：

```ts
import { describe, it, expect, beforeEach, vi } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useStore } from '../../store'
import { useImageComposer } from '../../hooks/useImageComposer'

vi.mock('../../lib/db', () => ({
  clearTasks: vi.fn(),
  clearImages: vi.fn(),
  getAllTasks: vi.fn().mockResolvedValue([]),
  getAllImages: vi.fn().mockResolvedValue([]),
}))

describe('useImageComposer', () => {
  beforeEach(() => {
    useStore.setState({
      prompt: '',
      inputImages: [],
      params: { size: 'auto', exact_size: false, quality: 'auto', output_format: 'png', output_compression: null, moderation: 'auto', n: 1, transparent_output: false },
      settings: { apiUrl: 'https://example.com', apiKey: 'k', model: 'gpt-image-1' } as any,
    })
  })

  it('exposes prompt and setPrompt wired to the store', () => {
    const { result } = renderHook(() => useImageComposer())
    expect(result.current.prompt).toBe('')
    act(() => result.current.setPrompt('一只猫'))
    expect(useStore.getState().prompt).toBe('一只猫')
  })

  it('exposes params.n and setParams wired to the store', () => {
    const { result } = renderHook(() => useImageComposer())
    expect(result.current.params.n).toBe(1)
    act(() => result.current.setParams({ n: 3 }))
    expect(useStore.getState().params.n).toBe(3)
  })

  it('exposes a submitCurrentMode function', () => {
    const { result } = renderHook(() => useImageComposer())
    expect(typeof result.current.submitCurrentMode).toBe('function')
  })
})
```

注意：测试只验证 hook 正确接线到 store，不验证 `submitTask` 的内部（那已有 `store.test.ts` 覆盖）。

- [ ] **Step 2: 验证测试失败**

```bash
npm test -- useImageComposer
```
Expected: FAIL（`Cannot find module '../../hooks/useImageComposer'`）。

- [ ] **Step 3: 实现 hook（从 InputBar 搬移）**

`src/hooks/useImageComposer.ts`：

**实现策略**：打开 `src/components/InputBar.tsx`，**逐段搬移**以下代码到 hook 内，保持变量名和依赖不变：

1. **store 选择器**（`:443-474` 中与 composer 相关的：prompt/setPrompt/inputImages/addInputImage/replaceInputImage/removeInputImage/clearInputImages/moveInputImage/params/setParams/settings/setSettings/appMode/activeAgentConversationId）→ 搬进 hook 顶部
2. **派生计算**（`:754-860`：currentActiveProfile/activeProfile/effectiveSettings/isFalProvider/.../displaySize/outputImageLimit/atImageLimit/uploadImageTooltipText/baseGenerationStrategyItems/generationStrategyItems/asset4K*）→ 搬进 hook，用 `useMemo`/裸计算保持原样
3. **本地输入镜像**（`:746-750`：outputCompressionInput/nInput/nInputFocused 及其 setter）→ 搬进 hook（用 `useState`）
4. **同步 effects**（`:1011-1019`：compression/n 同步回 params；`:1021-1027`：auto-normalize）→ 搬进 hook（用 `useEffect`）
5. **commit 回调**（`:1056-1085`：commitOutputCompression/commitN；`:1087-1128`：applyAsset4K*）→ 搬进 hook（用 `useCallback`）
6. **提交**（`:778-787`：submitCurrentMode/stopActiveAgentResponse + hasSubmitApiConfig/activeAgentIsRunning 派生）→ 搬进 hook
7. **上传**（`:1226-1258`：handleFiles）→ 搬进 hook（用 `useCallback`，依赖 addImageFromFile + API_MAX_IMAGES）

最后 `return { ...all of the above }`。

**关键纪律**：
- **不**搬 contentEditable 相关（`syncPromptFromContentEditable` `:788`、`cursorPos` `:710`、`menuLeft` `:711`、`atImageMenu*` `:695-696`、`textareaRef`）—— 这些是桌面 InputBar 的纯 UI，留在组件里。
- **不**搬批量选择栏（`selectedTaskIds` 等）—— 那是 gallery 的事，不属于 composer。
- **不**搬 refs（`fileInputRef`/`cameraInputRef` 等）—— 它们属于具体 UI。

> 这一步代码量大，但本质是"剪切粘贴 + 加 return"。如果某个变量不知道该不该搬，原则：**它是否同时被桌面 InputBar 和移动 MobileComposeSheet 都需要？是 → 搬；否 → 留**。

- [ ] **Step 4: 验证测试通过**

```bash
npm test -- useImageComposer
```
Expected: PASS。

- [ ] **Step 5: 改造 InputBar 消费 hook**

`src/components/InputBar.tsx`：

1. 顶部加 `import { useImageComposer } from '../hooks/useImageComposer'`
2. 在 `InputBar()` 函数体开头，把第 1-6 步搬走的代码段**删除**，替换为：
   ```ts
   const composer = useImageComposer()
   const {
     prompt, setPrompt, inputImages, addInputImage, removeInputImage, clearInputImages,
     params, setParams, displaySize, outputImageLimit, atImageLimit, uploadImageTooltipText,
     moderationDisabled, transparentOutputAvailable, showTransparentOutputControl,
     transparentOutputEnabled, compressionDisabled, exactSizeDisabled, exactSizeEnabled,
     outputCompressionInput, setOutputCompressionInput, nInput, setNInput,
     applyAsset4KOriginalRatioPreset, applyAsset4KRatioPreset,
     submitCurrentMode, stopActiveAgentResponse, hasSubmitApiConfig, activeAgentIsRunning,
     handleFiles, baseGenerationStrategyItems, generationStrategyItems,
   } = composer
   ```
3. 保留在 InputBar 里的 contentEditable / refs / 批量选择 / agent UI 代码原样不动。
4. 检查：被解构出的变量在 InputBar JSX 里的所有引用点都还能解析（因为变量名没变）。

- [ ] **Step 6: 行为等价回归（关键）**

```bash
npm test
npm run lint
npm run build
```
Expected: 全过。然后**手动验证桌面端**：
```bash
npm run dev
```
在浏览器 ≥640px 宽度下打开，确认 InputBar 的所有功能（输入 prompt、@ 挂图、改尺寸/质量/格式/数量、上传、生成、4K 预设、当前生成策略展示）**与重构前完全一致**。

> 这是 Task 2 的硬门：任何桌面端行为差异都必须在进入 Task 3 前解决。

- [ ] **Step 7: 提交**

```bash
git add src/hooks/useImageComposer.ts src/components/__tests__/useImageComposer.test.ts src/components/InputBar.tsx
git commit -m "Extract useImageComposer hook from InputBar

Behavior-equivalent extraction of prompt/params/derivation/submit/upload
logic into src/hooks/useImageComposer.ts. Desktop InputBar now consumes
the hook; all variable names preserved so JSX references are unchanged.
Desktop behavior verified identical at >=640px. Prepares for mobile
MobileComposeSheet to share the same logic."
```

---

## Task 3: `useMobileSheet` hook（手势 + 键盘适配）

**目标**：创作抽屉 / 详情 / 设置都需要"从底部滑入、下滑关闭、键盘弹出时按钮可见"。抽一个通用 hook。

**Files:**
- Create: `src/hooks/useMobileSheet.ts`
- Create: `src/components/__tests__/useMobileSheet.test.ts`

**Interfaces:**
- Produces:
  ```ts
  export interface MobileSheetOptions {
    open: boolean
    onClose: () => void
    enabled?: boolean        // 默认 true；false 时 hook 不生效（供测试 / 桌面端短路）
    dismissThreshold?: number // 默认 80（px），下滑超过此值触发 onClose
  }
  export interface MobileSheetController {
    sheetRef: React.RefObject<HTMLDivElement>       // 绑到 sheet 根元素
    dragTranslateY: number                          // 当前下拉偏移（px），用于 transform
    onDragStart: (e: React.PointerEvent) => void
    onDragMove: (e: React.PointerEvent) => void
    onDragEnd: () => void
    keyboardInset: number                           // visualViewport.height 变化时给底部的让位
  }
  export function useMobileSheet(options: MobileSheetOptions): MobileSheetController
  ```

- [ ] **Step 1: 写失败测试**

`src/components/__tests__/useMobileSheet.test.ts`：

```ts
import { describe, it, expect, vi } from 'vitest'
import { renderHook } from '@testing-library/react'
import { useMobileSheet } from '../../hooks/useMobileSheet'

describe('useMobileSheet', () => {
  it('returns zero translate when closed', () => {
    const { result } = renderHook(() =>
      useMobileSheet({ open: false, onClose: () => {} }),
    )
    expect(result.current.dragTranslateY).toBe(0)
  })

  it('calls onClose when drag exceeds threshold', () => {
    const onClose = vi.fn()
    const { result } = renderHook(() => useMobileSheet({ open: true, onClose }))
    // 模拟下拉 120px（>默认阈值 80）
    result.current.onDragStart({ clientY: 100, pointerId: 1 } as any)
    result.current.onDragMove({ clientY: 220, pointerId: 1 } as any)
    result.current.onDragEnd()
    expect(onClose).toHaveBeenCalledTimes(1)
  })

  it('does not close when drag below threshold', () => {
    const onClose = vi.fn()
    const { result } = renderHook(() => useMobileSheet({ open: true, onClose }))
    result.current.onDragStart({ clientY: 100, pointerId: 1 } as any)
    result.current.onDragMove({ clientY: 140, pointerId: 1 } as any) // 只拉了 40
    result.current.onDragEnd()
    expect(onClose).not.toHaveBeenCalled()
  })

  it('is inert when enabled=false', () => {
    const onClose = vi.fn()
    const { result } = renderHook(() =>
      useMobileSheet({ open: true, onClose, enabled: false }),
    )
    result.current.onDragStart({ clientY: 100, pointerId: 1 } as any)
    result.current.onDragMove({ clientY: 500, pointerId: 1 } as any)
    result.current.onDragEnd()
    expect(onClose).not.toHaveBeenCalled()
  })
})
```

- [ ] **Step 2: 验证失败**

```bash
npm test -- useMobileSheet
```
Expected: FAIL（module not found）。

- [ ] **Step 3: 实现**

`src/hooks/useMobileSheet.ts`：

```ts
import { useCallback, useEffect, useRef, useState } from 'react'

export interface MobileSheetOptions {
  open: boolean
  onClose: () => void
  enabled?: boolean
  dismissThreshold?: number
}

export interface MobileSheetController {
  sheetRef: React.RefObject<HTMLDivElement>
  dragTranslateY: number
  onDragStart: (e: React.PointerEvent) => void
  onDragMove: (e: React.PointerEvent) => void
  onDragEnd: () => void
  keyboardInset: number
}

export function useMobileSheet(options: MobileSheetOptions): MobileSheetController {
  const { open, onClose, enabled = true, dismissThreshold = 80 } = options
  const sheetRef = useRef<HTMLDivElement>(null)
  const [dragTranslateY, setDragTranslateY] = useState(0)
  const [keyboardInset, setKeyboardInset] = useState(0)
  const dragStartY = useRef<number | null>(null)
  const dragging = useRef(false)

  // 仅在 open 时生效
  const active = enabled && open

  const onDragStart = useCallback(
    (e: React.PointerEvent) => {
      if (!active) return
      // 只在抓手/sheet 顶部触发；这里简化为整个 sheet 可拖。
      // 调用方可绑定到抓手元素以限制触发区。
      dragStartY.current = e.clientY
      dragging.current = true
      ;(e.target as Element).setPointerCapture?.(e.pointerId)
    },
    [active],
  )

  const onDragMove = useCallback(
    (e: React.PointerEvent) => {
      if (!dragging.current || dragStartY.current === null) return
      const dy = e.clientY - dragStartY.current
      if (dy > 0) setDragTranslateY(dy) // 只允许下拉
    },
    [],
  )

  const onDragEnd = useCallback(() => {
    if (!dragging.current) return
    dragging.current = false
    if (dragTranslateY > dismissThreshold) {
      onClose()
    }
    setDragTranslateY(0)
    dragStartY.current = null
  }, [dragTranslateY, dismissThreshold, onClose])

  // 键盘适配：visualViewport 变化时，给底部留出键盘高度
  useEffect(() => {
    if (!active) {
      setKeyboardInset(0)
      return
    }
    const vv = window.visualViewport
    if (!vv) return
    const update = () => {
      const inset = window.innerHeight - vv.height - vv.offsetTop
      setKeyboardInset(inset > 0 ? inset : 0)
    }
    update()
    vv.addEventListener('resize', update)
    vv.addEventListener('scroll', update)
    return () => {
      vv.removeEventListener('resize', update)
      vv.removeEventListener('scroll', update)
    }
  }, [active])

  return { sheetRef, dragTranslateY, onDragStart, onDragMove, onDragEnd, keyboardInset }
}
```

- [ ] **Step 4: 验证通过**

```bash
npm test -- useMobileSheet
```
Expected: PASS（4 个 it）。

- [ ] **Step 5: 提交**

```bash
git add src/hooks/useMobileSheet.ts src/components/__tests__/useMobileSheet.test.ts
git commit -m "Add useMobileSheet hook for bottom-sheet gestures + keyboard

Provides drag-to-dismiss (configurable threshold) and visualViewport
keyboard-inset tracking. Shared by MobileComposeSheet, DetailModal,
SettingsModal mobile variants."
```

---

## Task 4: `MobileShell` —— 顶栏 + 内容槽 + TabBar + FAB

**目标**：移动端的外壳骨架。在 `<sm` 下替代 `<Header/> + <main> + <InputBar/>` 的桌面布局。

**Files:**
- Create: `src/components/MobileShell.tsx`
- Modify: `src/App.tsx`（条件渲染）

**Interfaces:**
- Consumes: `useIsMobile()`、store 的 `appMode`/`setAppMode`/`filterFavorite`/`setFilterFavorite`/`activeFavoriteCollectionId`/`setShowSettings`/`setSupportPromptOpen`/`setDetailTaskId` 等
- Produces: `export default function MobileShell({ children, onOpenCompose }: { children: ReactNode; onOpenCompose: () => void })`

- [ ] **Step 0: 确认/添加 `.safe-area-bottom` CSS 工具类**

勘查结果：`src/index.css:23` 已有 CSS 变量 `--safe-area-bottom: env(safe-area-inset-bottom, 0px)`，也有 `.safe-area-x`（`:409`）和 `.safe-area-top`（`:414`）工具类，但**没有 `.safe-area-bottom` 工具类**。本计划多处用到它，需先补上。

在 `src/index.css` 的 `.safe-area-top` 定义之后（约 `:416` 附近）加：

```css
.safe-area-bottom {
  padding-bottom: var(--safe-area-bottom);
}
```

确认方法：
```bash
grep -n "safe-area-bottom" src/index.css
```
应看到变量定义（`:23`）和新加的工具类定义两处。

**Tab 设计**（spec §3.1）：

| Tab | 行为 |
|---|---|
| 画廊 | `setAppMode('gallery')` + `setFilterFavorite(false)` |
| 收藏 | `setAppMode('gallery')` + `setFilterFavorite(true)` |
| ✨（FAB）| 调 `onOpenCompose()` 打开创作抽屉 |
| 我的 | 打开「我的」面板（设置入口、主题、安装、指南、统计） |

- [ ] **Step 1: 实现 MobileShell（无需单测——纯展示组件，靠 e2e 验收）**

`src/components/MobileShell.tsx`：

```tsx
import { useState, type ReactNode } from 'react'
import { Image as ImageIcon, Star, Sparkles, User, Moon, Sun } from 'lucide-react'
import { useStore } from '../store'
import { useIsMobile } from '../hooks/useIsMobile'

interface MobileShellProps {
  children: ReactNode
  onOpenCompose: () => void
}

export default function MobileShell({ children, onOpenCompose }: MobileShellProps) {
  const appMode = useStore((s) => s.appMode)
  const setAppMode = useStore((s) => s.setAppMode)
  const filterFavorite = useStore((s) => s.filterFavorite)
  const setFilterFavorite = useStore((s) => s.setFilterFavorite)
  const setShowSettings = useStore((s) => s.setShowSettings)
  const [myOpen, setMyOpen] = useState(false)

  const goGallery = () => {
    setAppMode('gallery')
    setFilterFavorite(false)
  }
  const goFavorites = () => {
    setAppMode('gallery')
    setFilterFavorite(true)
  }

  // 当前激活 tab（用于高亮）
  const tab = appMode === 'agent' ? 'agent' : filterFavorite ? 'favorites' : 'gallery'

  return (
    <div className="max-sm:flex max-sm:min-h-screen max-sm:flex-col sm:hidden">
      {/* 极简顶栏 */}
      <header className="safe-area-top sticky top-0 z-30 flex items-center justify-between border-b border-stone-200/70 bg-[#f7f2ec]/90 px-4 py-2 backdrop-blur dark:border-white/[0.08] dark:bg-[#13100d]/90">
        <span className="text-[15px] font-bold tracking-tight text-stone-900 dark:text-stone-50">TaoStudio</span>
        <ThemeToggleButton />
      </header>

      {/* 内容区 */}
      <main className="flex-1 overflow-y-auto pb-24">{children}</main>

      {/* 右下角 FAB + 底部 Tab */}
      <div className="safe-area-bottom fixed inset-x-0 bottom-0 z-40 border-t border-stone-200/70 bg-white/95 backdrop-blur dark:border-white/[0.08] dark:bg-[#13100d]/95">
        <div className="relative flex items-center justify-around px-2 py-1.5">
          <TabButton icon={ImageIcon} label="画廊" active={tab === 'gallery'} onClick={goGallery} />
          <TabButton icon={Star} label="收藏" active={tab === 'favorites'} onClick={goFavorites} />
          <div className="w-14" /> {/* FAB 占位 */}
          <TabButton icon={User} label="我的" active={myOpen} onClick={() => setMyOpen(true)} />
        </div>
        {/* FAB */}
        <button
          onClick={onOpenCompose}
          aria-label="创作"
          className="absolute -top-7 left-1/2 -translate-x-1/2 flex h-14 w-14 items-center justify-center rounded-full bg-[#df7b57] text-white shadow-lg ring-4 ring-white dark:ring-[#13100d] active:scale-95 transition"
        >
          <Sparkles className="h-6 w-6" />
        </button>
      </div>

      {/* 「我的」面板（简化：列出设置入口；本任务只做骨架） */}
      {myOpen && <MyPanel onClose={() => setMyOpen(false)} onOpenSettings={() => setShowSettings(true)} />}
    </div>
  )
}

function TabButton({ icon: Icon, label, active, onClick }: {
  icon: React.ComponentType<{ className?: string }>
  label: string
  active: boolean
  onClick: () => void
}) {
  return (
    <button
      onClick={onClick}
      className={`flex flex-1 flex-col items-center gap-0.5 rounded-lg py-1.5 ${
        active ? 'text-[#df7b57]' : 'text-stone-500 dark:text-stone-400'
      }`}
    >
      <Icon className="h-5 w-5" />
      <span className="text-[10px] font-medium">{label}</span>
    </button>
  )
}

function ThemeToggleButton() {
  // 复用现有主题切换逻辑的最小版本；实际应调用 store 里现有的 toggle，避免重复实现
  // TODO（实现时确认）：Header.tsx 里主题切换调的 store action 名，这里直接 import
  const isDark = document.documentElement.classList.contains('dark')
  const toggle = () => document.documentElement.classList.toggle('dark')
  return (
    <button onClick={toggle} aria-label="切换主题" className="rounded-lg p-2 text-stone-600 dark:text-stone-300">
      {isDark ? <Sun className="h-5 w-5" /> : <Moon className="h-5 w-5" />}
    </button>
  )
}

function MyPanel({ onClose, onOpenSettings }: { onClose: () => void; onOpenSettings: () => void }) {
  return (
    <div className="fixed inset-0 z-50 flex items-end" onClick={onClose}>
      <div className="absolute inset-0 bg-black/40" />
      <div
        className="safe-area-bottom relative w-full rounded-t-2xl bg-white p-4 dark:bg-[#1a1612]"
        onClick={(e) => e.stopPropagation()}
      >
        <div className="mx-auto mb-3 h-1 w-10 rounded-full bg-stone-300 dark:bg-stone-600" />
        <h3 className="mb-3 text-sm font-semibold text-stone-900 dark:text-stone-50">我的</h3>
        <button onClick={onOpenSettings} className="block w-full rounded-lg px-3 py-3 text-left text-sm text-stone-700 active:bg-stone-100 dark:text-stone-200 dark:active:bg-white/5">
          设置
        </button>
        {/* 其余入口（主题/安装/指南/历史/智能体）在后续任务补 */}
      </div>
    </div>
  )
}
```

> **实现注意**：`ThemeToggleButton` 里我用了简化的 DOM 直接操作作为占位。**实现时必须先查 `Header.tsx` 里主题切换实际调的 store action**（大概率是 `useStore` 里某个 `toggleTheme` 或 `setTheme`），改成调它，避免行为不一致。这是本任务唯一的"实现时确认点"。

- [ ] **Step 2: App.tsx 装配（移动端走 MobileShell，桌面端走原路径）**

`src/App.tsx`：在 `return` 里改造为：

```tsx
const isMobile = useIsMobile()
const [composeOpen, setComposeOpen] = useState(false)

return (
  <>
    {isMobile ? (
      <MobileShell onOpenCompose={() => setComposeOpen(true)}>
        {/* 复用现有内容渲染逻辑：appMode === 'agent' ? <AgentWorkspace/> : <画廊> */}
        {/* 把现有 <main> 内的内容搬到这里（不含 InputBar，InputBar 在桌面才渲染） */}
      </MobileShell>
    ) : (
      <>
        <Header />
        {appMode === 'agent' ? <Suspense><AgentWorkspace /></Suspense> : (
          <main data-home-main data-drag-select-surface className="pb-48">
            <div className="safe-area-x max-w-7xl mx-auto">
              <GalleryWorkspaceHeader />
              <SearchBar />
              {filterFavorite && !activeFavoriteCollectionId
                ? <Suspense><FavoriteCollectionsView /></Suspense>
                : <Suspense fallback={<div className="min-h-[220px]" />}><TaskGrid /></Suspense>}
            </div>
          </main>
        )}
        <InputBar />
      </>
    )}

    {/* 模态层：两端共用，不变 */}
    <Suspense fallback={null}>
      {detailTaskId ? <DetailModal /> : null}
      ...
    </Suspense>
    <Toast />
    <ImageContextMenu />
  </>
)
```

> **关键**：桌面分支（`else`）的 JSX **逐字节等于现状**，只是从顶层 `return` 搬进 `else`。移动分支的内容渲染复用同样的组件（TaskGrid/SearchBar/AgentWorkspace），但**不渲染 `<InputBar/>`**（移动端用 MobileComposeSheet 替代，Task 5 实现）。本任务先把 `<InputBar/>` 也临时不渲染会导致移动端没法生成图 —— 这是预期的，因为创作抽屉在 Task 5 才接上。**Task 4 完成时移动端能看 Tab/画廊/我的，但不能生成**，这是可接受的中间态。

- [ ] **Step 3: 验证**

```bash
npm test && npm run lint && npm run build
```
Expected: 全过。然后：
```bash
npm run dev
```
浏览器 DevTools 切到 390×844：应看到顶栏（TaoStudio + 主题图标）、画廊内容、底部 Tab（画廊/收藏/FAB/我的）、FAB 可点（此时还不触发抽屉，Task 5 接）、「我的」能打开面板。切回 ≥640px：应看到**完全未变的桌面布局**。

- [ ] **Step 4: 提交**

```bash
git add src/components/MobileShell.tsx src/App.tsx
git commit -m "Add MobileShell skeleton: top bar + content + bottom Tab + FAB

Renders only at <640px via useIsMobile. Desktop (>=640px) branch is
byte-identical to current App.tsx return. Compose sheet not yet wired
(Task 5); mobile can browse gallery/favorites/my but cannot generate
until Task 5 lands."
```

---

## Task 5: `MobileComposeSheet` —— 创作抽屉 + 参数胶囊 + 更多 sheet

**目标**：移动端的主力交互——从底部滑出的全屏创作模式。复用 `useImageComposer`，UI 按参数降级表（spec §4）。

**Files:**
- Create: `src/components/MobileComposeSheet.tsx`
- Create: `src/components/MobileMoreParamsSheet.tsx`
- Modify: `src/App.tsx`（把 FAB 的 `onOpenCompose` 接到本组件的 open 状态）

**Interfaces:**
- Consumes: `useImageComposer()`、`useMobileSheet()`、`useStore` 的 `showSettings` setter（生成失败/未配置时打开设置）
- Produces: `export default function MobileComposeSheet({ open, onClose }: { open: boolean; onClose: () => void })`

- [ ] **Step 1: 实现 MobileMoreParamsSheet（二级 sheet，先做依赖项）**

`src/components/MobileMoreParamsSheet.tsx`：

```tsx
import { useImageComposer } from '../hooks/useImageComposer'
import { useMobileSheet } from '../hooks/useMobileSheet'

export default function MobileMoreParamsSheet({ open, onClose }: { open: boolean; onClose: () => void }) {
  const {
    params, setParams,
    transparentOutputEnabled, showTransparentOutputControl,
    exactSizeEnabled, exactSizeDisabled,
    moderationDisabled,
  } = useImageComposer()
  const sheet = useMobileSheet({ open, onClose })

  if (!open) return null

  return (
    <div className="fixed inset-0 z-[60] flex items-end sm:hidden" onClick={onClose}>
      <div className="absolute inset-0 bg-black/50" />
      <div
        ref={sheet.sheetRef}
        style={{ transform: `translateY(${sheet.dragTranslateY}px)`, paddingBottom: sheet.keyboardInset }}
        onClick={(e) => e.stopPropagation()}
        onPointerDown={sheet.onDragStart}
        onPointerMove={sheet.onDragMove}
        onPointerUp={sheet.onDragEnd}
        className="safe-area-bottom relative w-full max-h-[80vh] overflow-y-auto rounded-t-2xl bg-white p-4 dark:bg-[#1a1612]"
      >
        <div className="mx-auto mb-3 h-1 w-10 rounded-full bg-stone-300 dark:bg-stone-600" />
        <h3 className="mb-3 text-sm font-semibold text-stone-900 dark:text-stone-50">更多参数</h3>

        <Row label="格式">
          <Seg
            value={params.output_format}
            options={[['png', 'PNG'], ['jpeg', 'JPG'], ['webp', 'WebP']]}
            onChange={(v) => setParams({ output_format: v as any })}
          />
        </Row>

        {showTransparentOutputControl && (
          <Row label="透明背景">
            <Toggle on={transparentOutputEnabled} onClick={() => setParams({ transparent_output: !transparentOutputEnabled })} />
          </Row>
        )}

        {!moderationDisabled && (
          <Row label="审核">
            <Seg
              value={params.moderation}
              options={[['auto', '自动'], ['low', '宽松']]}
              onChange={(v) => setParams({ moderation: v as any })}
            />
          </Row>
        )}

        {!exactSizeDisabled && (
          <Row label="精确尺寸">
            <Toggle on={exactSizeEnabled} onClick={() => setParams({ exact_size: !exactSizeEnabled })} />
          </Row>
        )}
      </div>
    </div>
  )
}

function Row({ label, children }: { label: string; children: React.ReactNode }) {
  return (
    <div className="flex items-center justify-between border-b border-stone-100 py-3 dark:border-white/5">
      <span className="text-sm text-stone-700 dark:text-stone-200">{label}</span>
      {children}
    </div>
  )
}
function Seg({ value, options, onChange }: { value: string; options: [string, string][]; onChange: (v: string) => void }) {
  return (
    <div className="flex gap-1 rounded-lg bg-stone-100 p-0.5 dark:bg-white/5">
      {options.map(([v, l]) => (
        <button key={v} onClick={() => onChange(v)} className={`rounded-md px-3 py-1 text-xs ${value === v ? 'bg-white text-stone-900 shadow dark:bg-white/15 dark:text-stone-50' : 'text-stone-500'}`}>{l}</button>
      ))}
    </div>
  )
}
function Toggle({ on, onClick }: { on: boolean; onClick: () => void }) {
  return (
    <button onClick={onClick} className={`relative h-6 w-11 rounded-full transition ${on ? 'bg-[#df7b57]' : 'bg-stone-300 dark:bg-stone-600'}`}>
      <span className={`absolute top-0.5 h-5 w-5 rounded-full bg-white transition ${on ? 'left-[22px]' : 'left-0.5'}`} />
    </button>
  )
}
```

- [ ] **Step 2: 实现 MobileComposeSheet（主抽屉）**

`src/components/MobileComposeSheet.tsx`：

```tsx
import { useState } from 'react'
import { ImageUp, Sparkles, ChevronDown } from 'lucide-react'
import { useImageComposer } from '../hooks/useImageComposer'
import { useMobileSheet } from '../hooks/useMobileSheet'
import { useStore } from '../store'
import MobileMoreParamsSheet from './MobileMoreParamsSheet'

export default function MobileComposeSheet({ open, onClose }: { open: boolean; onClose: () => void }) {
  const composer = useImageComposer()
  const {
    prompt, setPrompt, inputImages, removeInputImage,
    params, setParams, nInput, setNInput,
    submitCurrentMode, hasSubmitApiConfig, activeAgentIsRunning, stopActiveAgentResponse,
    handleFiles, atImageLimit, uploadImageTooltipText,
    applyAsset4KOriginalRatioPreset,
  } = composer
  const setShowSettings = useStore((s) => s.setShowSettings)
  const sheet = useMobileSheet({ open, onClose })
  const [moreOpen, setMoreOpen] = useState(false)

  if (!open) return null

  const onSubmit = () => {
    if (activeAgentIsRunning) return stopActiveAgentResponse()
    if (hasSubmitApiConfig) return submitCurrentMode()
    setShowSettings(true)
  }

  return (
    <div className="fixed inset-0 z-50 flex items-end sm:hidden">
      <div className="absolute inset-0 bg-black/50" onClick={onClose} />
      <div
        ref={sheet.sheetRef}
        style={{ transform: `translateY(${sheet.dragTranslateY}px)`, paddingBottom: sheet.keyboardInset }}
        className="safe-area-bottom relative flex max-h-[92vh] w-full flex-col rounded-t-2xl bg-[#f7f2ec] dark:bg-[#13100d]"
      >
        {/* 抓手 */}
        <div
          className="flex shrink-0 cursor-grab items-center justify-center py-2 active:cursor-grabbing"
          onPointerDown={sheet.onDragStart}
          onPointerMove={sheet.onDragMove}
          onPointerUp={sheet.onDragEnd}
        >
          <div className="h-1 w-10 rounded-full bg-stone-300 dark:bg-stone-600" />
        </div>

        {/* 可滚动内容 */}
        <div className="flex-1 overflow-y-auto px-4 pb-2">
          {/* 大输入框 */}
          <textarea
            value={prompt}
            onChange={(e) => setPrompt(e.target.value)}
            placeholder="描述你想生成的图片，可输入 @ 来指定参考图..."
            rows={4}
            className="w-full resize-none rounded-xl border border-stone-200 bg-white p-3 text-base text-stone-900 placeholder:text-stone-400 focus:border-[#df7b57] focus:outline-none dark:border-white/10 dark:bg-white/5 dark:text-stone-50"
          />

          {/* 上传 + 已挂图预览 */}
          <div className="mt-3 flex items-center gap-2">
            <label className={`flex items-center gap-1.5 rounded-lg border border-stone-200 bg-white px-3 py-2 text-sm text-stone-700 dark:border-white/10 dark:bg-white/5 dark:text-stone-200 ${atImageLimit ? 'opacity-50' : ''}`}>
              <ImageUp className="h-4 w-4" />
              上传图片
              <input type="file" accept="image/*" multiple className="hidden" disabled={atImageLimit}
                onChange={(e) => e.target.files && handleFiles(e.target.files)} />
            </label>
            <span className="text-xs text-stone-400">{uploadImageTooltipText}</span>
          </div>
          {inputImages.length > 0 && (
            <div className="mt-2 flex gap-2 overflow-x-auto">
              {inputImages.map((img, i) => (
                <div key={img.id} className="relative shrink-0">
                  <img src={img.dataUrl} alt="" className="h-14 w-14 rounded-lg object-cover" />
                  <button onClick={() => removeInputImage(img.id)} className="absolute -right-1 -top-1 h-5 w-5 rounded-full bg-stone-800 text-xs text-white">×</button>
                </div>
              ))}
            </div>
          )}

          {/* 一行胶囊：尺寸 / 质量 / 数量 / 4K / 更多 */}
          <div className="mt-3 flex flex-wrap gap-2">
            <Chip label="尺寸" value={params.size === 'auto' ? '自动' : params.size} onClick={() => {/* TODO Task 6: 复用 SizePickerModal，或弹原生 select */}} />
            <Chip label="质量" value={params.quality === 'auto' ? '自动' : params.quality} onClick={() => {
              const next = params.quality === 'auto' ? 'high' : params.quality === 'high' ? 'medium' : params.quality === 'medium' ? 'low' : 'auto'
              setParams({ quality: next as any })
            }} />
            <Chip label="数量" value={`×${nInput || params.n}`} onClick={() => {
              const next = Math.min(4, (parseInt(nInput) || params.n) + 1)
              setNInput(String(next))
            }} />
            <button onClick={applyAsset4KOriginalRatioPreset} className="rounded-full border border-stone-200 bg-white px-3 py-1.5 text-xs text-stone-600 dark:border-white/10 dark:bg-white/5 dark:text-stone-300">
              4K
            </button>
            <button onClick={() => setMoreOpen(true)} className="rounded-full border border-stone-200 bg-white px-3 py-1.5 text-xs text-stone-600 dark:border-white/10 dark:bg-white/5 dark:text-stone-300">
              + 更多
            </button>
          </div>
        </div>

        {/* 主按钮（底部固定） */}
        <div className="shrink-0 border-t border-stone-200/60 p-3 dark:border-white/5">
          <button
            onClick={onSubmit}
            className="flex w-full items-center justify-center gap-2 rounded-xl bg-[#df7b57] py-3 text-base font-semibold text-white active:scale-[0.99]"
          >
            <Sparkles className="h-5 w-5" />
            {activeAgentIsRunning ? '停止' : '生成图像'}
          </button>
        </div>
      </div>

      <MobileMoreParamsSheet open={moreOpen} onClose={() => setMoreOpen(false)} />
    </div>
  )
}

function Chip({ label, value, onClick }: { label: string; value: string; onClick: () => void }) {
  return (
    <button onClick={onClick} className="flex items-center gap-1 rounded-full border border-stone-200 bg-white px-3 py-1.5 text-xs text-stone-700 dark:border-white/10 dark:bg-white/5 dark:text-stone-200">
      <span className="text-stone-400">{label}</span>
      <span className="font-medium">{value}</span>
      <ChevronDown className="h-3 w-3 text-stone-400" />
    </button>
  )
}
```

> **实现注意（尺寸胶囊）**：代码里 `尺寸` Chip 的 onClick 是占位 `/* TODO */`。**实现时**应弹出现有的 `SizePickerModal`（`src/components/SizePickerModal.tsx`，已存在）—— 加一个 `sizePickerOpen` 本地 state，渲染 `<SizePickerModal open={sizePickerOpen} ... />`。先读 `SizePickerModal` 的 props 签名再接线，不要猜。

- [ ] **Step 3: App.tsx 接入创作抽屉**

`src/App.tsx`：把 Task 4 里 `onOpenCompose={() => setComposeOpen(true)}` 的 `composeOpen` state 接到本组件：

```tsx
// 在 MobileShell 的 children 外（或 MobileShell 之后）加：
{isMobile && <MobileComposeSheet open={composeOpen} onClose={() => setComposeOpen(false)} />}
```

`MobileComposeSheet` 用 `import { lazy, Suspense } from 'react'` 懒加载：
```tsx
const MobileComposeSheet = lazy(() => import('./components/MobileComposeSheet'))
```

- [ ] **Step 4: 验证**

```bash
npm test && npm run lint && npm run build
```
Expected: 全过。然后 `npm run dev`，390 视口：点 FAB → 创作抽屉从底部滑入 → 可输入 prompt、上传图、点胶囊改质量/数量、点 4K、点「+ 更多」展开二级 sheet、点「生成图像」触发 `submitCurrentMode`（实际生成走现有 store/API 链路，与桌面等价）。下滑抽屉顶部抓手 → 关闭。

- [ ] **Step 5: 提交**

```bash
git add src/components/MobileComposeSheet.tsx src/components/MobileMoreParamsSheet.tsx src/App.tsx
git commit -m "Add MobileComposeSheet: bottom-up full-screen compose drawer

Consumes useImageComposer (shared with desktop InputBar). Parameter
demotion: 尺寸/质量/数量/4K 常驻 as chips, 格式/透明/审核/精确尺寸 in
'+更多' secondary sheet. Drag-to-dismiss via useMobileSheet. Main button
fixed above keyboard (visualViewport inset). Wired to FAB in App.tsx."
```

---

## Task 6: 画廊移动适配（单列 + sticky 搜索 + 统计降级）

**目标**：画廊 Tab 在 `<sm` 下为单列大图卡片、搜索折叠、统计只露关键 3 个。

**Files:**
- Modify: `src/components/TaskGrid.tsx`
- Modify: `src/components/SearchBar.tsx`
- Modify: `src/App.tsx`（`GalleryWorkspaceHeader` 的统计行）

- [ ] **Step 1: TaskGrid 单列**

`src/components/TaskGrid.tsx`：找到现有 grid 容器的 className，把列数在 `<sm` 下改为单列。当前大概率是 `grid-cols-2 md:grid-cols-3 ...` 之类。改成：

```tsx
className="grid grid-cols-1 gap-3 sm:grid-cols-2 md:grid-cols-3 ..."
```

> **实现时**：先读 `TaskGrid.tsx` 找到实际的 grid className（Grep `grid-cols`），在前面加 `grid-cols-1` 并保留原有 `sm:`/`md:` 断点。不改动任何卡片内部渲染。

- [ ] **Step 2: TaskCard 移动点击区**

`src/components/TaskCard.tsx`：在 `<sm` 下加大字号、保证点击区 ≥44px。主要通过在卡片外层 className 加 `max-sm:p-3 max-sm:active:bg-stone-100` 之类调整，不改逻辑。

> **实现时**：审查 TaskCard 现有 className，只加 `max-sm:` 前缀的微调，不重构。

- [ ] **Step 3: SearchBar sticky 折叠**

`src/components/SearchBar.tsx`：在 `<sm` 下默认折叠为一个搜索图标按钮，点开才展开输入框。最小实现：

```tsx
// 在 SearchBar 内加本地 state
const [expanded, setExpanded] = useState(false)
// <sm 下：未展开时只显示一个放大镜按钮；展开时显示输入框
```

> **实现时**：读 SearchBar 现状（4.8KB，应该不复杂），用 `max-sm:` 控制：`max-sm:hidden`（默认隐藏输入区）+ 一个 `sm:hidden` 的图标按钮切换 `expanded`。

- [ ] **Step 4: 统计降级**

`src/App.tsx` 的 `GalleryWorkspaceHeader`：现有 `statItems` 数组（6 项）在 `<sm` 下只露「输出 / 生成中 / 收藏」3 项。

```tsx
const statItemsMobile = statItems.filter((item) => ['输出', '生成中', '收藏'].includes(item.label))
// 渲染时：max-sm: 用 statItemsMobile，sm: 用 statItems
```

具体用 `useIsMobile()` 选数组，或用 CSS `hidden sm:flex` / `max-sm:hidden` 双渲染。

- [ ] **Step 5: 验证 + 提交**

```bash
npm test && npm run lint && npm run build
```
390 视口确认：画廊单列、搜索折叠、统计 3 项。≥640px 确认：**完全未变**。

```bash
git add src/components/TaskGrid.tsx src/components/TaskCard.tsx src/components/SearchBar.tsx src/App.tsx
git commit -m "Mobile-adapt gallery: single column, collapsible search, reduced stats

<sm: grid-cols-1, search collapses to icon, stat header shows only
输出/生成中/收藏. >=sm: byte-identical to before."
```

---

## Task 7: 模态移动适配（DetailModal / Lightbox / SettingsModal 全屏 sheet）

**目标**：三个模态在 `<sm` 下全屏 sheet 化。

**Files:**
- Modify: `src/components/DetailModal.tsx`
- Modify: `src/components/Lightbox.tsx`
- Modify: `src/components/SettingsModal.tsx`

**通用模式**：每个模态的根容器在 `<sm` 下：
1. `max-sm:inset-0 max-sm:rounded-none`（全屏不圆角）
2. 顶部加可下滑的抓手（用 `useMobileSheet`）
3. 主操作按钮 sticky 在底部安全区上方

- [ ] **Step 1: DetailModal 全屏 sheet**

`src/components/DetailModal.tsx`（67KB，大文件，只改根容器 + 加抓手）：

```tsx
// 顶部 import
import { useMobileSheet } from '../hooks/useMobileSheet'
import { useIsMobile } from '../hooks/useIsMobile'

// 组件内
const isMobile = useIsMobile()
const sheet = useMobileSheet({ open: true, onClose: () => /* 现有的关闭逻辑 */, enabled: isMobile })

// 根容器 className 加：
//   max-sm:inset-0 max-sm:rounded-none max-sm:translate-y-0
// style 加：transform: translateY(sheet.dragTranslateY)
// 顶部加抓手 div（绑定 sheet 的 onPointerDown/Move/Up）
```

> **实现时**：先 Grep 找到 DetailModal 的根 `<div>` 和关闭函数名（大概率 `onClose` 或 `setDetailTaskId(null)`），再改。不动内部内容渲染。

- [ ] **Step 2: Lightbox 全屏 + 左右滑**

`src/components/Lightbox.tsx`：根容器 `max-sm:inset-0 max-sm:rounded-none`。多图切换的左右滑：复用现有 `onNext`/`onPrev`（应该已有），加 touch swipe：

```tsx
// 简单的横向 swipe 检测（不必抽 hook，Lightbox 独有）
const touchStartX = useRef(0)
onTouchStart={(e) => touchStartX.current = e.touches[0].clientX}
onTouchEnd={(e) => {
  const dx = e.changedTouches[0].clientX - touchStartX.current
  if (dx > 50) onPrev?.()
  if (dx < -50) onNext?.()
}}
```

> **实现时**：确认 Lightbox 现有的 next/prev 函数名再接线。

- [ ] **Step 3: SettingsModal 全屏 sheet（仅包裹，不拆内容）**

`src/components/SettingsModal.tsx`（126KB，**只改根容器**）：

```tsx
// 根容器 className 加：
//   max-sm:inset-0 max-sm:rounded-none max-sm:max-h-screen
// 顶部加抓手（useMobileSheet，enabled = isMobile）
// 底部主操作（保存/关闭）加 max-sm:sticky max-sm:bottom-0
```

**严格不碰内部内容**（spec §8.2：拆分留作后续独立计划）。

- [ ] **Step 4: 验证 + 提交**

```bash
npm test && npm run lint && npm run build
```
390 视口：点画廊卡片 → DetailModal 全屏、可下滑关闭；点图 → Lightbox 全屏、左右滑切换；「我的」→ 设置 → SettingsModal 全屏。≥640px：**完全未变**。

```bash
git add src/components/DetailModal.tsx src/components/Lightbox.tsx src/components/SettingsModal.tsx
git commit -m "Mobile-adapt modals: full-screen sheets with drag-to-dismiss

DetailModal/Lightbox/SettingsModal: at <sm become inset-0 full-screen
sheets with grab-handle drag-to-dismiss (useMobileSheet). Lightbox adds
horizontal swipe for multi-image. SettingsModal content unchanged (split
deferred to separate plan). >=sm: byte-identical."
```

---

## Task 8: 「我的」完善 + Agent/Mask 降级提示

**目标**：把 Task 4 里 `MyPanel` 的占位补全（主题/安装/指南/历史/智能体入口），并给 Agent/Mask 在移动端加降级提示。

**Files:**
- Modify: `src/components/MobileShell.tsx`（补全 `MyPanel`）
- Modify: `src/App.tsx`（agent 模式移动端入口提示）
- Modify: `src/components/DetailModal.tsx`（Mask 编辑按钮移动端提示）

- [ ] **Step 1: 补全 MyPanel**

`src/components/MobileShell.tsx` 的 `MyPanel`：加入入口列表（复用现有 store action / 现有组件）：

```tsx
function MyPanel({ onClose, onOpenSettings }: ...) {
  const setShowSettings = useStore((s) => s.setShowSettings)
  const setSupportPromptOpen = useStore((s) => s.setSupportPromptOpen)  // 操作指南（确认实际名）
  const setAppMode = useStore((s) => s.setAppMode)
  // 统计：从 store.tasks 算 total/done/saved（复用 App.tsx GalleryWorkspaceHeader 的 reduce）

  const items = [
    { label: '设置', onClick: () => { setShowSettings(true); onClose() } },
    { label: '操作指南', onClick: () => { setSupportPromptOpen(true); onClose() } },
    { label: '智能体工作台', onClick: () => { /* 提示"建议桌面端"，确认后 setAppMode('agent') */ } },
    // 历史、安装为应用 按需加
  ]
  // 渲染列表 + 顶部统计摘要
}
```

> **实现时确认点**：`setSupportPromptOpen` 的实际 store action 名（HelpModal 触发的是哪个）；"安装为应用"按钮的现有逻辑在 `Header.tsx`，提取其 onClick 复用。

- [ ] **Step 2: Agent 入口降级提示**

`src/App.tsx`：当 `isMobile && appMode === 'agent'` 时，在 AgentWorkspace 外层加一个提示条：

```tsx
{isMobile && appMode === 'agent' && (
  <div className="max-sm:m-3 max-sm:rounded-lg max-sm:border max-sm:border-amber-300 max-sm:bg-amber-50 max-sm:p-3 max-sm:text-xs max-sm:text-amber-800">
    智能体工作台为多图工作流，建议在桌面端使用以获得更好体验。
  </div>
)}
```

不改 AgentWorkspace 本身，仍可进入。

- [ ] **Step 3: Mask 编辑降级提示**

`src/components/DetailModal.tsx`：找到"编辑遮罩/Mask"按钮，在 `<sm` 下点击时不打开 `MaskEditorModal`，而是 `showToast('遮罩编辑建议在桌面端使用')`。

```tsx
const onMaskEdit = isMobile
  ? () => showToast('遮罩编辑建议在桌面端使用')
  : () => setMaskEditorImageId(imgId)
```

> **实现时**：找到 DetailModal 里 Mask 按钮的现有 onClick，按 `isMobile` 分支。

- [ ] **Step 4: 验证 + 提交**

```bash
npm test && npm run lint && npm run build
```
390 视口：「我的」面板列表完整；进智能体有提示条；点 Mask 按钮弹 toast 而非打开编辑器。

```bash
git add src/components/MobileShell.tsx src/App.tsx src/components/DetailModal.tsx
git commit -m "Complete '我的' panel + Agent/Mask mobile demotion notices

MyPanel: settings/guide/agent/stats entries. Agent mode shows 'recommend
desktop' banner on mobile (still accessible). Mask edit button on mobile
shows toast instead of opening MaskEditorModal (deferred to desktop)."
```

---

## Task 9: 端到端验收 + gates

**目标**：全链路走查，确认 spec §9 所有验收项达成。

- [ ] **Step 1: 移动端 e2e 走查（390×844）**

```bash
npm run dev
```
DevTools → 390×844，逐项核对 spec §9：
- [ ] 主屏：底部 Tab + 全屏画廊 + 右下 FAB
- [ ] 点 FAB → 创作抽屉滑入，含大输入框 + 3 胶囊 + 生成按钮
- [ ] 「+ 更多」→ 二级 sheet（格式/透明/审核/精确尺寸）
- [ ] 4K 小入口在尺寸旁
- [ ] 画廊单列、搜索折叠、统计 3 项
- [ ] 「我的」含设置/指南/统计/智能体
- [ ] DetailModal 全屏可下滑关闭；Lightbox 全屏左右滑；SettingsModal 全屏
- [ ] 安全区：Tab Bar 和生成按钮不被 Home Indicator 遮挡
- [ ] 键盘弹出时生成按钮可见
- [ ] 输入 prompt → 生成 → 结果出现在画廊（实际跑一次生成，验证与桌面等价的端到端链路）

- [ ] **Step 2: 桌面端回归（≥1280px）**

切回桌面宽度，逐项确认**与重构前完全一致**：
- [ ] Header 三段式布局未变
- [ ] InputBar 固定底栏、所有参数行、"当前生成策略"块、4K 块都在
- [ ] 点 InputBar 的生成按钮，行为正常
- [ ] 画廊、DetailModal、SettingsModal 桌面形态未变

- [ ] **Step 3: 全部 gates**

```bash
npm test
npm run lint
npm run build
```
Expected: 三项全过（lint 零错误）。

- [ ] **Step 4: 可选 verify:ui（如果生产可访问）**

```bash
npm run verify:ui -- https://image.taostudioai.com/
```

- [ ] **Step 5: 最终提交（如有验收中修的小问题）**

```bash
git add -A
git commit -m "Mobile UX rebuild: end-to-end verification pass"
```

---

## Self-Review（plan 作者自查）

**1. Spec coverage**（逐节对照 spec）：

| Spec 章节 | 实现 Task |
|---|---|
| §1 诊断 | （背景，无实现） |
| §2 IA（底部 Tab + FAB + 抽屉） | Task 4（外壳）+ Task 5（抽屉） |
| §3 Tab 导航细节 | Task 4（TabBar）+ Task 8（我的完善） |
| §4 创作抽屉参数降级表 | Task 5（主抽屉）+ Task 5 Step 1（更多 sheet） |
| §5 画廊/详情/设置适配 | Task 6（画廊）+ Task 7（模态） |
| §6.1 断点策略 | Global Constraints + 所有 Task 用 `max-sm:`/`sm:` |
| §6.2 新增技术件 | Task 1（useIsMobile）+ Task 2（useImageComposer）+ Task 3（useMobileSheet）+ Task 4/5（4 个组件） |
| §6.3 InputBar 重构边界 | Task 2 |
| §6.4 安全区/键盘 | Task 3（useMobileSheet 的 keyboardInset）+ Task 4/5 用 `safe-area-bottom` |
| §6.5 PWA 强化 | **未覆盖** —— spec 标注"可选，低优先"。已在下方 Gaps 说明 |
| §8.2 SettingsModal 不拆 | Task 7 Step 3（仅包裹） |
| §8.2 Mask/Agent 降级 | Task 8 Step 2/3 |

**Gaps（已知未覆盖）**：
- **§6.5 PWA 强化（"添加到主屏幕"引导）**：spec 明确标"可选，低优先"。Task 8 的 MyPanel 里"安装为应用"入口会复用 Header 现有逻辑，但不在本计划做新引导组件。如需，可作为 Task 8 的可选 Step。
- ~~**`safe-area-bottom` CSS 工具类**~~：**已修正**。勘查确认 `src/index.css` 只有变量 `--safe-area-bottom`（`:23`）而无同名工具类；已在 Task 4 加 Step 0（新增 `.safe-area-bottom` 工具类）。

**2. Placeholder scan**：
- Task 4 `ThemeToggleButton` 有"实现时确认 store action 名"标注 —— 这是合理的不确定点（需要读 Header.tsx 确认），不是占位符空洞。已明确指示。
- Task 5 `尺寸` Chip onClick 是 TODO —— 已指示读 `SizePickerModal` props 接线。
- Task 8 多处"确认实际 action 名" —— 同上，属合理勘查型指示。
- 无 "TBD"/"add error handling"/"similar to Task N" 空洞。

**3. Type consistency**：
- `useImageComposer` 在 Task 2 定义的返回字段（displaySize/outputImageLimit/...）与 Task 5 MobileComposeSheet 解构的字段一致。✓
- `useMobileSheet` 在 Task 3 定义的 `MobileSheetController`（sheetRef/dragTranslateY/onDragStart/Move/End/keyboardInset）与 Task 4/5/7 使用的字段一致。✓
- `useIsMobile(): boolean` 在 Task 1 定义，Task 4/7/8 消费 `boolean`。✓
- `MobileShell({children, onOpenCompose})` 在 Task 4 定义，Task 5 的 App.tsx 接入用同一签名。✓

**修正项**：~~在 Task 4 Step 1 前补确认 `.safe-area-bottom`~~ **已落地**：Task 4 Step 0 已加，`src/index.css` 新增 `.safe-area-bottom` 工具类。

---

## Execution Handoff

Plan 完成，保存于 `docs/superpowers/plans/2026-07-06-mobile-ux.md`。两种执行方式：

**1. Subagent-Driven（推荐）** —— 我每个 Task 派一个新 subagent 实现，任务间我做 review，迭代快、上下文干净。

**2. Inline Execution** —— 在当前会话按 Task 顺序执行，批量推进 + 检查点 review。

你选哪种？
